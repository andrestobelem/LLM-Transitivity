[project]
name = "llm-transitivity"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
]

[tool.ruff]
target-version = "py314"
